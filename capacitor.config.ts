import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hy22-bio-composter',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
